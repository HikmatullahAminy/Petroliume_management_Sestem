@model IEnumerable<AHMSApplicationDemo.Models.PurchaseSalles>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Salles)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Purchase)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Salles.SallesId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Purchase.PurchaseId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PurchaseSallesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PurchaseSallesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PurchaseSallesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
